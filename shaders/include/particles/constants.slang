public module ParticleConstants;

static const float32_t boundLength = 100.0f * 1.0f;
static const float32_t halfBoundLength = boundLength / 2.0f - 1.0f * 0.25f;

static const float32_t deltaTime = 1.0f / 60.0f;

static const float32_t smoothingLength = 3.0f;
